// Shared GA4 constants for the Lotus seller funnel. Kept in one file so the
// CTA island and the modal cannot drift apart on page_category, which is what
// every report on this funnel will group by.

export const ANALYSIS_PAGE_CATEGORY = 'listing-8804-skyward-street'
